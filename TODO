- Melhorar o desempenho de geração de nounces (criá-los previamente e mantê-los
no banco).
- Verificar porque o OpenID está utilizando o modo inseguro de conexão (sem
associação).
- Usar os dados do LDAP para armazenar os dados do usuário.
- Tirar o sreg do Store.

- Tabela de sites está com dados errados. Melhor revisar essa parte do código.
- Adicionar função de logout (que apague os cookies de todas as aplicações do
domínio).
- Usar criptografia no cookie.
- Create user information on the fly if the LDAP authentication succeeds.
